"""REMI adapter modules.

Concrete implementations of the abstract interfaces. Each subpackage has a
`base.py` defining the interface and one file per concrete implementation.

Adapters are wired up at startup by config/settings.py based on environment.
"""
